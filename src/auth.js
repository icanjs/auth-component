import { connect } from 'react-view-models';
import DefineMap from 'can-define/map/';
import View from './auth.jsx';
import './auth.less';

export const ViewModel = DefineMap.extend({
});

import View fromexport default connect(ViewModel, View);
